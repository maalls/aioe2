# Test

la structure des dossiers fichiers test doit reflecter la structure du fichier tester, avec pour path/fichier.py tests/path/fichier/test_function1.py etc

en cas de modification de code, mettre a jour les tests ou creer les tests si il manque.

le fichier test devrait etre generé avant le code, et mise a jour des tests quand le code est mise a jour.
